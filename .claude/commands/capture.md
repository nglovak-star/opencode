---
description: Quickly capture an idea, note, or piece of information without derailing current work
argument-hint: <what you want to capture>
---

Read `glovak-executive-assistant/core/instructions.md` first.

Captured input: $ARGUMENTS

1. Decide where this belongs:
   - Sounds like a new idea/opportunity → add to
     `glovak-executive-assistant/ideas/idea-inbox.md` (status `INBOX`).
   - Sounds like reference information worth keeping → save into the
     right `glovak-executive-assistant/knowledge/<category>/` folder.
   - Sounds like a short-lived reminder → add to
     `glovak-executive-assistant/memory/temporary-context.md`.
   - Sounds like an actual task → suggest adding it via `/tasks` instead.
2. File it, say where it went in one line, and check whether there's an
   unfinished P1/P2 priority in
   `glovak-executive-assistant/tasks/tasks.md` to return to. If so, name it,
   per the tone in `core/instructions.md` (don't let a new idea silently
   replace today's priority).

This is a low-risk organizational action — no confirmation needed.
