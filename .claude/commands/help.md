---
description: List available commands and how this system works
---

Read `glovak-executive-assistant/README.md` and
`glovak-executive-assistant/START-HERE.md`, then present:

1. The list of commands and what each does (from `README.md`'s Commands
   table): `/briefing`, `/tasks`, `/projects`, `/ideas`, `/review`,
   `/decision`, `/research`, `/capture`, `/focus`, `/status`, `/help`.
2. A one-line reminder that plain-language requests work too — commands are
   shortcuts, not required syntax.
3. A one-line reminder of the guardrails: nothing gets sent, deleted,
   purchased, or published without confirmation first.

Keep this brief — it's a reference, not an essay.
